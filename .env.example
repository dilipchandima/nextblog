# When migrating from Next.js on Vercel, copy variables from:
# Vercel → Project → Settings → Environment Variables
#
# Client-side values that used NEXT_PUBLIC_* should be renamed to VITE_*
# and referenced as import.meta.env.VITE_* in application code.
